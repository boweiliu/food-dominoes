using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cart : MonoBehaviour
{
    Stack<GameObject> CartStorage;
    // Start is called before the first frame update
    void Start()
    {
        CartStorage = new Stack<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AddToCart(GameObject platter)
    {

    }

    public void RemoveFromCart(GameObject platter)
    {

    }
}
